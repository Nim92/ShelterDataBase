<%- include("../partials/header") %>

	<div class="row">
		<h1 style="text-align: center">Rediger <%= shelter.name%></h1>
		<div style="width: 30%; margin: 20px auto;">
			<form action="/shelters/<%= shelter._id %>?_method=PUT" method="POST">
				<div class="form-group">
					<input class="form-control" type="text" name="shelter[name]" value="<%= shelter.name %>">
				</div>
				<div class="form-group">
					<input class="form-control" type="text" name="shelter[image]" value="<%= shelter.image %>">
				</div>
				<div class="form-group">
					<textarea class="form-control" name="shelter[description]" cols="40" rows="5"><%= shelter.description %></textarea>
				</div>
				<div class="form-group">
					<button class="btn btn-lg btn-primary btn-block">Tilføj</button>
				</div>
			</form>
			<a href="/shelters">Tilbage</a>
		</div>
	</div>


<%- include("../partials/footer") %>